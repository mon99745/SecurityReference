### 해당 프로젝트 경로 (default: ~)
root-dir: ~
db-file-dir: ${root-dir}/db/h2/h2db

### 서버 관리
server:
### 내장 WAS 관리
  port: 8080

### 토큰 관리
jwt:
  secret: VlwEyVBsYt9V7zq57TejMnVUyzblYcfPQye08f7MGVA9XkHa

### Spring 관리
spring:
  datasource:
### h2 DB(default)
    driver-class-name: org.h2.Driver
###    다중 프로세스 접근 (File Mode)
    url: jdbc:h2:file:${db-file-dir};AUTO_SERVER=true
###    단일 프로세스 접근 (In-Memory Mode)
#    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console

### JPA 관리
  jpa:
    hibernate.ddl-auto: update
    show-sql: true

### Security 관리
  security:
    user:
      name: admin
      password: admin